# ──────────────────────────────────────────────────────────────
# Hyprland Environment Variables (Non-NVIDIA)
# ──────────────────────────────────────────────────────────────
# Copy this file to env.conf and adjust as needed.
#
# On NVIDIA systems, you may want to add:
#   env = LIBVA_DRIVER_NAME,nvidia
#   env = __GLX_VENDOR_LIBRARY_NAME,nvidia
#   env = __GL_VRR_ALLOWED,0
#
# On AMD/Intel systems, these are typically not needed.
# ──────────────────────────────────────────────────────────────

env = XCURSOR_SIZE,24
env = HYPRCURSOR_SIZE,24
